// Homepage.jsx
import React from 'react';

const Homepage = ({ children }) => {
    return (
        <div>
            {children} {/* This renders everything passed to Homepage */}
        </div>
    );
};

export default Homepage;
